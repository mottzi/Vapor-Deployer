# Codex Instructions

When the user asks to `deploy` this project, perform the deployment workflow:

1. Review the current changes and create an appropriate git commit unsing a single git command if possible.
2. Push the current branch.
3. Run `ssh root@mottzi.codes deployerctl update`.

Your only output should be: Commited and deployed (SHA short)

These files exemplify prefered code formating styles:

OperationEngine.swift

Where possible, try to adhere this code style.
